import { DecimalPipe } from '@angular/common';

export class Sale {

    idSale: number; 
    idItem: number =0;
    nameBuyer: string ="";
    amount: number;
    totalSale:number;
    dateSale: string="";
   
}